using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class Background : MonoBehaviour
{
    // Start is called before the first frame update
    void Start()
    {
        
    }

    void onToogle(bool show)
    {
        this.enabled = show;
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
